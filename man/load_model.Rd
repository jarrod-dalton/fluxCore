% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/load_model.R
\name{load_model}
\alias{load_model}
\title{Assemble and validate a simulation model (v2.0.0 API)}
\usage{
load_model(
  schema,
  bundle,
  policy = NULL,
  environment = NULL,
  trajectory = NULL,
  runtime = NULL,
  param_source = NULL
)
}
\arguments{
\item{schema}{A validated schema list (from \code{\link[=set_schema]{set_schema()}} or equivalent),
\strong{or} a schema-like named list. Must include at minimum a \verb{$variables}
field and a \verb{$time_spec} of class \code{"time_spec"}. See \code{\link[=set_schema]{set_schema()}}.}

\item{bundle}{A ModelBundle list with at minimum \code{propose_events},
\code{transition}, and \code{stop} callbacks.}

\item{policy}{Optional. A function or list with a \code{propose_action} method
called at declared decision points. Stage 2B.}

\item{environment}{Optional. An \link{EnvironmentContext} for ABM/RL scenarios.}

\item{trajectory}{Optional. A TrajectoryLogger configuration list; enables
\link{TrajectoryRecord} emission. Requires \code{schema$decision_points} to be
non-empty.}

\item{runtime}{Optional. A \link{RuntimeContext} specifying reproducibility and
backend settings. Defaults are applied when \code{NULL}.}

\item{param_source}{Optional. A ParamSource that resolves
\link{ParamContext}s once per run.}
}
\value{
An \link{Engine} object with \code{v2_mode = TRUE}.
}
\description{
\code{load_model()} is the recommended entry point for the v2.0.0 architecture.
It validates all supplied components against the schema and against each
other, applies defaults, and returns a configured \link{Engine} in v2 mode.
}
\details{
Engines returned by \code{load_model()} enforce the following at runtime:
\itemize{
\item \strong{Fail fast on \code{ctx}-style usage}: passing a \verb{ctx=} argument to
\code{Engine$run()} raises an immediate error. Use typed context objects
(\code{SimContext}, \code{ParamContext}, \code{RuntimeContext}) instead.
\item All bundle callbacks must accept \code{sim_ctx} and \code{param_ctx} in their
formals (warnings emitted for v1.x-style \code{ctx} formals).
}
}
\section{User experience tiers}{
\tabular{lll}{
   Level \tab Entry point \tab What you supply \cr
   1 \tab \code{load_model(schema, bundle)} \tab In-memory schema + bundle \cr
   2 \tab \code{load_model(schema, bundle, ...)} + JSON schema export \tab JSON spec + language code \cr
   3 \tab All optional components supplied \tab Full stack \cr
}
}

\section{Trajectory output contract}{

When \code{trajectory} is configured, the Engine returned by \code{load_model()} emits
\code{trajectory_records} in run outputs.
\itemize{
\item \code{Engine$run(...)} includes \code{trajectory_records} when trajectory logging is enabled.
\item \code{run_cohort(...)} run entries include per-run \code{trajectory_records} when enabled.
\item \code{trajectory_records} is a list of plain named lists (JSON-serializable).
\item \code{trajectory$detail} controls state capture:
\itemize{
\item \code{none}: \code{state_before} and \code{state_after} are \code{NULL}.
\item \code{summary}: both are outputs of \code{summary_fn} (default \code{\link[=state_summary_default]{state_summary_default()}}).
\item \code{full}: both are full snapshots of \code{entity$current}.
}
}
}

\seealso{
\code{\link[=SimContext]{SimContext()}}, \code{\link[=ParamContext]{ParamContext()}}, \code{\link[=RuntimeContext]{RuntimeContext()}},
\code{\link[=EnvironmentContext]{EnvironmentContext()}}, \code{\link[=DecisionPoint]{DecisionPoint()}}, \code{\link[=TrajectoryRecord]{TrajectoryRecord()}}
}
