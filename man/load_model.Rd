% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/load_model.R
\name{load_model}
\alias{load_model}
\title{Assemble and validate a simulation model (v2 API)}
\usage{
load_model(
  schema,
  bundle,
  policy = NULL,
  environment = NULL,
  trajectory = NULL,
  runtime = NULL,
  param_source = NULL
)
}
\arguments{
\item{schema}{A validated full schema list (from \code{\link[=set_schema]{set_schema()}} or
equivalent) containing at minimum \verb{$variables} and a \verb{$time_spec} of class
\code{"time_spec"}. For 2.1 compatibility, a variables-only named list is also
accepted with a migration warning. A full schema may contain canonical leaf
declarations in \verb{$decision_points} and grouped references in
\verb{$decision_groups}. See \code{\link[=set_schema]{set_schema()}}.}

\item{bundle}{A ModelBundle list with at minimum \code{propose_events},
\code{transition}, and \code{stop} callbacks, plus a \verb{$time_spec} semantically equal
to the full schema declaration.}

\item{policy}{Optional for ordinary decisions. A function or list with a
\code{propose_action} method called at ordinary decision points. A schema with
non-empty \code{decision_groups} requires a list with a \code{propose_plan} method.}

\item{environment}{Optional. An \link{EnvironmentContext} for ABM/RL scenarios.}

\item{trajectory}{Optional. A TrajectoryLogger configuration list; enables
\link{TrajectoryRecord} emission. Requires \code{schema$decision_points} to be
non-empty.}

\item{runtime}{Optional. A \link{RuntimeContext} specifying reproducibility and
backend settings. Defaults are applied when \code{NULL}.}

\item{param_source}{Optional. A ParamSource that resolves
\link{ParamContext}s once per run.}
}
\value{
An \link{Engine} object with \code{v2_mode = TRUE}.
}
\description{
\code{load_model()} is the recommended entry point for the v2 architecture.
It validates all supplied components against the schema and against each
other, applies defaults, and returns a configured \link{Engine} in v2 mode.
}
\details{
Engines returned by \code{load_model()} enforce the following at runtime:
\itemize{
\item \strong{Fail fast on \code{ctx}-style usage}: passing a \verb{ctx=} argument to
\code{Engine$run()} raises an immediate error. Use typed context objects
(\code{SimContext}, \code{ParamContext}, \code{RuntimeContext}) instead.
\item Bundle callbacks that need typed context declare supported \code{sim_ctx} and
\code{param_ctx} formals. The removed v1.x-style \code{ctx} formal is rejected.
}
}
\section{Model time contract}{

A full schema and its ModelBundle must declare semantically equal
\code{time_spec} objects. Equality covers unit, origin instant, origin class, and
zone; object identity is not required. \link{Engine}\verb{$time_spec} is the sole
assembled runtime clock and is propagated through \code{SimContext}.

For 2.1 compatibility, a variables-only schema (a named list of variable
definitions without the full \verb{$variables} wrapper) is accepted with a
migration warning and uses \code{bundle$time_spec}. A full schema, identified by
the presence of a \verb{$variables} field, must also contain \verb{$time_spec}.
}
\section{Decision declaration contract}{

Leaf \code{\link[=DecisionPoint]{DecisionPoint()}} objects remain in \code{schema$decision_points}. Shared
trigger declarations live separately in \code{schema$decision_groups} and
reference leaf ids rather than embedding leaf definitions. \code{load_model()}
defensively repeats the global id, membership, no-nesting, and group-only
trigger checks performed by \code{\link[=set_schema]{set_schema()}} so manually assembled schemas do
not bypass the declaration contract. When at least one group is declared,
\code{policy} must be a list exposing \code{propose_plan()}; grouped dispatch never
falls back implicitly to \code{propose_action()}.
}
\section{User experience tiers}{
\tabular{lll}{
   Level \tab Entry point \tab What you supply \cr
   1 \tab \code{load_model(schema, bundle)} \tab In-memory schema + bundle \cr
   2 \tab \code{load_model(schema, bundle, ...)} + JSON schema export \tab JSON spec + language code \cr
   3 \tab All optional components supplied \tab Full stack \cr
}
}

\section{Trajectory output contract}{

When \code{trajectory} is configured, the Engine returned by \code{load_model()} emits
\code{trajectory_records} in run outputs.
\itemize{
\item \code{Engine$run(...)} includes \code{trajectory_records} when trajectory logging is enabled.
\item \code{run_cohort(...)} run entries include per-run \code{trajectory_records} when enabled.
\item \code{trajectory_records} is a list of plain named lists (JSON-serializable).
\item \code{trajectory$detail} controls state capture:
\itemize{
\item \code{none}: \code{state_before} and \code{state_after} are \code{NULL}.
\item \code{summary}: both are outputs of \code{summary_fn} (default \code{\link[=state_summary_default]{state_summary_default()}}).
\item \code{full}: both are full snapshots of \code{entity$current}.
}
}
}

\seealso{
\code{\link[=SimContext]{SimContext()}}, \code{\link[=ParamContext]{ParamContext()}}, \code{\link[=RuntimeContext]{RuntimeContext()}},
\code{\link[=EnvironmentContext]{EnvironmentContext()}}, \code{\link[=DecisionPoint]{DecisionPoint()}}, \code{\link[=GroupedDecisionPoint]{GroupedDecisionPoint()}},
\code{\link[=DecisionPlan]{DecisionPlan()}}, \code{\link[=TrajectoryRecord]{TrajectoryRecord()}}
}
