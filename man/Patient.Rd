% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/Patient.R
\name{Patient}
\alias{Patient}
\title{Patient: schema-based R6 class for event-sourced state with implicit event index \code{j}}
\description{
The \code{Patient} class stores a patient's evolving state across simulation events.
It is designed for simulations where:
}
\details{
\itemize{
\item there may be \strong{many state attributes} (e.g., 50+),
\item only a subset changes at any event (sparse updates),
\item event times are \strong{non-uniform}, and
\item events have \strong{types} (including terminal events like \code{"death"}).
}
\subsection{Key design choice: \code{j} is implicit (auto-incremented)}{

You do \strong{not} pass \code{j} to \verb{$update()}. Instead, the class maintains \code{last_j}
internally and assigns the next event index as \code{j_next = last_j + 1}.
}

\subsection{No-update events}{

Use \code{patient$update(time, event_type, changes = NULL)} to record an event that does
not alter state. The event log advances, but cached state and sparse history are
unchanged.
}
}
\section{Fields}{

\itemize{
\item \code{schema} : Named list. Attribute definitions (\code{default}, \code{coerce}, \code{validate}).
\item \code{current} : Named list. Cached current values for all attributes in schema.
\item \code{hist} : Named list. Sparse history per attribute (\verb{$j}, \verb{$v}).
\item \code{last_j} : Integer. Last event index recorded.
\item \code{last_time} : Numeric. Last event time recorded.
\item \code{events} : Data frame. Event log with columns \code{j}, \code{time}, \code{event_type}.
}
}

\section{Methods}{

\itemize{
\item \code{initialize(init, schema = default_patient_schema(), time0 = 0, event_type0 = "init")}
\item \code{state(vars = NULL)} : Current state as named numeric vector (subsettable).
\item \code{as_list(vars = NULL)} : Current state as list (subsettable).
\item \code{update(time, event_type, changes = NULL)} : Record event + sparse updates (\strong{auto \code{j}}).
\item \code{state_at(j, vars = NULL)} : Reconstruct state at event \code{j} (subsettable).
}
}

\examples{
library(patientSimCore)
set.seed(42)

schema <- default_patient_schema()
p <- Patient$new(init = list(age = 55, miles_to_work = 10), schema = schema, time0 = 0)

p$update(time = 0.3, event_type = "visit", changes = list(age = 55.3))
p$update(time = 0.9, event_type = "check", changes = NULL)

p$state(c("age", "miles_to_work"))
p$state_at(2, vars = c("age", "miles_to_work"))

}
\section{Public fields}{
\if{html}{\out{<div class="r6-fields">}}
\describe{
\item{\code{schema}}{Named list. Variable definitions (\code{default}, \code{coerce}, \code{validate}).}

\item{\code{current}}{Named list. Current values for all variables in \code{schema}.}

\item{\code{hist}}{Named list. Sparse history per variable.}

\item{\code{last_j}}{Integer. Last event index recorded.}

\item{\code{last_time}}{Numeric. Last event time recorded.}

\item{\code{events}}{Data frame. Event log with columns \code{j}, \code{time}, \code{event_type}.}
}
\if{html}{\out{</div>}}
}
\section{Methods}{
\subsection{Public methods}{
\itemize{
\item \href{#method-Patient-new}{\code{Patient$new()}}
\item \href{#method-Patient-state}{\code{Patient$state()}}
\item \href{#method-Patient-as_list}{\code{Patient$as_list()}}
\item \href{#method-Patient-update}{\code{Patient$update()}}
\item \href{#method-Patient-state_at}{\code{Patient$state_at()}}
\item \href{#method-Patient-clone}{\code{Patient$clone()}}
}
}
\if{html}{\out{<hr>}}
\if{html}{\out{<a id="method-Patient-new"></a>}}
\if{latex}{\out{\hypertarget{method-Patient-new}{}}}
\subsection{Method \code{new()}}{
\subsection{Usage}{
\if{html}{\out{<div class="r">}}\preformatted{Patient$new(
  init,
  schema = default_patient_schema(),
  time0 = 0,
  event_type0 = "init"
)}\if{html}{\out{</div>}}
}

\subsection{Arguments}{
\if{html}{\out{<div class="arguments">}}
\describe{
\item{\code{init}}{Named list. Initial values for patient variables.}

\item{\code{schema}}{Named list. Variable definitions; see \code{\link[=default_patient_schema]{default_patient_schema()}}.}

\item{\code{time0}}{Numeric. Initial time.}

\item{\code{event_type0}}{Character. Initial event label (default \code{"init"}).}
}
\if{html}{\out{</div>}}
}
}
\if{html}{\out{<hr>}}
\if{html}{\out{<a id="method-Patient-state"></a>}}
\if{latex}{\out{\hypertarget{method-Patient-state}{}}}
\subsection{Method \code{state()}}{
\subsection{Usage}{
\if{html}{\out{<div class="r">}}\preformatted{Patient$state(vars = NULL)}\if{html}{\out{</div>}}
}

\subsection{Arguments}{
\if{html}{\out{<div class="arguments">}}
\describe{
\item{\code{vars}}{Character vector of variable names to return; \code{NULL} returns all.}
}
\if{html}{\out{</div>}}
}
}
\if{html}{\out{<hr>}}
\if{html}{\out{<a id="method-Patient-as_list"></a>}}
\if{latex}{\out{\hypertarget{method-Patient-as_list}{}}}
\subsection{Method \code{as_list()}}{
\subsection{Usage}{
\if{html}{\out{<div class="r">}}\preformatted{Patient$as_list(vars = NULL)}\if{html}{\out{</div>}}
}

\subsection{Arguments}{
\if{html}{\out{<div class="arguments">}}
\describe{
\item{\code{vars}}{Character vector of variable names to return; \code{NULL} returns all.}
}
\if{html}{\out{</div>}}
}
}
\if{html}{\out{<hr>}}
\if{html}{\out{<a id="method-Patient-update"></a>}}
\if{latex}{\out{\hypertarget{method-Patient-update}{}}}
\subsection{Method \code{update()}}{
\subsection{Usage}{
\if{html}{\out{<div class="r">}}\preformatted{Patient$update(time, event_type, changes = NULL)}\if{html}{\out{</div>}}
}

\subsection{Arguments}{
\if{html}{\out{<div class="arguments">}}
\describe{
\item{\code{time}}{Numeric. Event time for an update.}

\item{\code{event_type}}{Character. Event label for an update.}

\item{\code{changes}}{Named list of sparse updates; \code{NULL} means record event with no state changes.}
}
\if{html}{\out{</div>}}
}
}
\if{html}{\out{<hr>}}
\if{html}{\out{<a id="method-Patient-state_at"></a>}}
\if{latex}{\out{\hypertarget{method-Patient-state_at}{}}}
\subsection{Method \code{state_at()}}{
\subsection{Usage}{
\if{html}{\out{<div class="r">}}\preformatted{Patient$state_at(j, vars = NULL)}\if{html}{\out{</div>}}
}

\subsection{Arguments}{
\if{html}{\out{<div class="arguments">}}
\describe{
\item{\code{j}}{Integer. Event index used by \code{state_at()}.}

\item{\code{vars}}{Character vector of variable names to return; \code{NULL} returns all.}
}
\if{html}{\out{</div>}}
}
}
\if{html}{\out{<hr>}}
\if{html}{\out{<a id="method-Patient-clone"></a>}}
\if{latex}{\out{\hypertarget{method-Patient-clone}{}}}
\subsection{Method \code{clone()}}{
The objects of this class are cloneable with this method.
\subsection{Usage}{
\if{html}{\out{<div class="r">}}\preformatted{Patient$clone(deep = FALSE)}\if{html}{\out{</div>}}
}

\subsection{Arguments}{
\if{html}{\out{<div class="arguments">}}
\describe{
\item{\code{deep}}{Whether to make a deep clone.}
}
\if{html}{\out{</div>}}
}
}
}
