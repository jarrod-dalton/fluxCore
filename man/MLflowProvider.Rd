% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/providers.R
\name{MLflowProvider}
\alias{MLflowProvider}
\title{MLflowProvider (stub)}
\arguments{
\item{builder_fn}{Function that builds a ModelBundle given \code{model_spec}.}

\item{sampler_fn}{Optional function to sample global parameter draws.}

\item{model_spec}{Named list describing which model/bundle to load.}

\item{n_param_draws}{Integer. Number of global parameter draws to return.}

\item{...}{Additional arguments (reserved for future extension).}
}
\description{
Scaffold for MLflow-based workflows. \code{model_spec} should include at least
\code{model_uri} and optionally references to artifacts needed to build a bundle.
}
\details{
This provider delegates to \code{builder_fn(model_uri, artifacts, model_spec, ...)}.

You can also supply an optional \code{sampler_fn(model_uri, artifacts, model_spec, D, ...)}
to generate global parameter draws for draw-aware components (e.g., regression models
with \code{cov(beta)}).
}
\section{Fields}{

\describe{
\item{\code{builder_fn}}{Function that builds a ModelBundle given \code{model_spec}.}

\item{\code{sampler_fn}}{Optional function that returns parameter draws.}
}}

\examples{
library(patientSimCore)
prov <- MLflowProvider$new(
  builder_fn = function(model_uri, artifacts = NULL, model_spec = NULL) default_model_bundle()
)
bundle <- prov$load(list(model_uri = "models:/my_model@champion"))

}
