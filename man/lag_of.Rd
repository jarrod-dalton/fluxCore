% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/derive.R
\name{lag_of}
\alias{lag_of}
\title{Define a lagged feature from variable history}
\usage{
lag_of(
  name,
  target,
  k = 1,
  lookback_t = NULL,
  lookback_j = NULL,
  include_current = FALSE,
  force = FALSE,
  na_value = NA,
  clock = "time"
)
}
\arguments{
\item{name}{Name of the derived feature.}

\item{target}{A target created by \code{\link[=var]{var()}}.}

\item{k}{Positive integer lag order (1 = most recent prior value by default).}

\item{lookback_t}{Optional numeric lookback window on the time axis. Takes precedence over \code{lookback_j}.}

\item{lookback_j}{Optional integer lookback window in event-index units.}

\item{include_current}{Logical; include boundary event at \code{t}/\code{j} (default FALSE for lag semantics).}

\item{force}{Logical; if TRUE return \code{na_value} when insufficient history exists (default FALSE).}

\item{na_value}{Value to return when \code{force=TRUE} and insufficient history exists (default \code{NA}).}

\item{clock}{Which column in \code{patient$events} defines the time axis for \code{lookback_t} (default \code{"time"}).}
}
\value{
A function \verb{(patient, j, t) -> scalar or NULL}.
}
\description{
\code{lag_of()} returns a function that extracts the k-th most recent value
of a variable from its sparse history, optionally within a lookback window.
}
