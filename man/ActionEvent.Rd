% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/decision_points.R
\name{ActionEvent}
\alias{ActionEvent}
\title{Construct an ActionEvent}
\usage{
ActionEvent(
  action_type,
  time_next,
  decision_point_id = NULL,
  params = NULL,
  metadata = NULL
)
}
\arguments{
\item{action_type}{Character scalar; must match an \code{allowed_actions} entry if
the decision point declared any.}

\item{time_next}{Numeric scalar; realization time on the canonical timeline.}

\item{decision_point_id}{Character scalar; which decision point produced this
action. If \code{NULL} (the default), the engine fills this in automatically
from the firing decision point's \code{id}. During a policy call, an explicitly
supplied value must exactly match the firing decision point. Outside policy
dispatch, the field may be used to construct a self-described action.}

\item{params}{Optional named list of action-specific parameters.}

\item{metadata}{Optional named list for policy provenance or audit fields.}
}
\value{
A list of class \code{"ActionEvent"}.
}
\description{
Timeline-native action proposed by a policy at a decision point. ActionEvents
enter the same arbitration and refresh lifecycle as all other events — no
side-channel state mutation.
}
