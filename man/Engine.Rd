% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/engine.R
\name{Engine}
\alias{Engine}
\title{Engine: orchestrate patient simulation using a ModelProvider + model_spec}
\description{
The \code{Engine} runs simulations by orchestrating the interaction between:
}
\details{
\itemize{
\item a mutable \code{Patient} object (state + history + event log), and
\item a \code{ModelBundle} (functions that propose events, apply transitions, and stop).
}
\subsection{Key design: Engine owns model materialization}{

Instead of passing a bundle directly, you pass:
\itemize{
\item a \code{provider} (e.g., \link{PackageProvider}, \link{FileProvider}, \link{MLflowProvider}), and
\item a \code{model_spec} (a named list describing which model/version/artifacts to load).
}

This keeps your simulation code stable as you evolve how models are stored, versioned,
and retrained.
}
}
\section{Bundle functions}{

A ModelBundle is a named list containing:
\itemize{
\item \code{propose_event(patient, ctx)} -> list(time_next, event_type, ...)
\item \code{transition(patient, event_type, time_next, ctx)} -> named list \code{changes} or NULL
\item \code{stop(patient, event_type, ctx)} -> logical scalar
\item optional \code{observe(patient, event_type, ctx)} -> list/row; accumulated by Engine
}
}

\section{Fields}{

\itemize{
\item \code{provider}: A ModelProvider (e.g., \link{PackageProvider}) that can load a model bundle.
\item \code{model_spec}: Named list describing which bundle to load.
\item \code{bundle}: The loaded ModelBundle (list of functions).
}
}

\section{Methods}{

\itemize{
\item \code{initialize(provider = PackageProvider$new(), model_spec = list(name = "default"), ...)}
\item \code{run(patient, max_events = 1000, max_time = NULL, return_observations = TRUE, ctx = NULL)}
}
}

\examples{
library(patientSimCore)
set.seed(1)

prov <- PackageProvider$new()
eng <- Engine$new(provider = prov, model_spec = list(name = "default"))

p <- Patient$new(init = list(age = 55, miles_to_work = 10), schema = default_patient_schema(), time0 = 0)
out <- eng$run(p, max_events = 200)
tail(out$events, 3)

}
\section{Methods}{
\subsection{Public methods}{
\itemize{
\item \href{#method-Engine-new}{\code{Engine$new()}}
\item \href{#method-Engine-run}{\code{Engine$run()}}
\item \href{#method-Engine-clone}{\code{Engine$clone()}}
}
}
\if{html}{\out{<hr>}}
\if{html}{\out{<a id="method-Engine-new"></a>}}
\if{latex}{\out{\hypertarget{method-Engine-new}{}}}
\subsection{Method \code{new()}}{
\subsection{Usage}{
\if{html}{\out{<div class="r">}}\preformatted{Engine$new(
  provider = PackageProvider$new(),
  model_spec = list(name = "default"),
  ...
)}\if{html}{\out{</div>}}
}

}
\if{html}{\out{<hr>}}
\if{html}{\out{<a id="method-Engine-run"></a>}}
\if{latex}{\out{\hypertarget{method-Engine-run}{}}}
\subsection{Method \code{run()}}{
\subsection{Usage}{
\if{html}{\out{<div class="r">}}\preformatted{Engine$run(
  patient,
  max_events = 1000,
  max_time = NULL,
  return_observations = TRUE,
  ctx = NULL
)}\if{html}{\out{</div>}}
}

}
\if{html}{\out{<hr>}}
\if{html}{\out{<a id="method-Engine-clone"></a>}}
\if{latex}{\out{\hypertarget{method-Engine-clone}{}}}
\subsection{Method \code{clone()}}{
The objects of this class are cloneable with this method.
\subsection{Usage}{
\if{html}{\out{<div class="r">}}\preformatted{Engine$clone(deep = FALSE)}\if{html}{\out{</div>}}
}

\subsection{Arguments}{
\if{html}{\out{<div class="arguments">}}
\describe{
\item{\code{deep}}{Whether to make a deep clone.}
}
\if{html}{\out{</div>}}
}
}
}
