% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/grouped_decisions.R
\name{GroupedDecisionPoint}
\alias{GroupedDecisionPoint}
\title{Construct a grouped decision point}
\usage{
GroupedDecisionPoint(id, trigger, members, label = NULL)
}
\arguments{
\item{id}{Non-empty character scalar. Group and leaf ids share one
schema-wide namespace.}

\item{trigger}{Non-empty character vector of event types or a predicate
function \verb{function(event)}. Unlike a group-only leaf, a grouped decision
point must own a trigger.}

\item{members}{Character vector containing at least two distinct leaf
decision-point ids. Declaration order is preserved and is the canonical
member order.}

\item{label}{Optional human-readable character scalar.}
}
\value{
A list of class \code{"GroupedDecisionPoint"}.
}
\description{
Declares one shared trigger that opens a coordinated policy consultation
across existing leaf \code{\link[=DecisionPoint]{DecisionPoint()}} objects. Group members are references
to ids in \code{schema$decision_points}; a grouped declaration does not copy leaf
action contracts or own pending-action slots.

When the group trigger fires, the Engine applies the triggering event's
transition once and then evaluates each member's \code{\link[=DecisionPoint]{DecisionPoint()}} condition
against the post-transition Entity. Members with no condition, or a condition
returning \code{TRUE}, are eligible and are presented together in declared member
order to \code{policy$propose_plan()}. An empty eligible set skips the policy call.
}
