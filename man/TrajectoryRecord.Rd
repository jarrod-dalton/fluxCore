% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/decision_points.R
\name{TrajectoryRecord}
\alias{TrajectoryRecord}
\title{Construct a TrajectoryRecord}
\usage{
TrajectoryRecord(
  run_id,
  entity_id,
  t,
  decision_point_id,
  observation,
  realized_event,
  candidate_actions = NULL,
  proposed_actions = NULL,
  selected_action = NULL,
  state_before = NULL,
  state_after = NULL,
  condition_met = NULL,
  reward = NULL
)
}
\arguments{
\item{run_id}{Character scalar; run identifier (from \code{SimContext}).}

\item{entity_id}{Character scalar; entity identifier.}

\item{t}{Numeric scalar; time at which the decision point fired.}

\item{decision_point_id}{Character scalar; which decision point produced
this record.}

\item{observation}{Named list; output of the decision point's
\code{observation_fn}.}

\item{realized_event}{The event record that triggered the decision point.}

\item{candidate_actions}{Optional character vector of actions presented to
the policy.}

\item{proposed_actions}{Optional list of actions proposed by the policy.}

\item{selected_action}{Optional \code{ActionEvent}; the action realized on the
timeline.}

\item{state_before}{Optional named list; entity state before the event.
\code{NULL} unless the TrajectoryLogger is configured to capture it.}

\item{state_after}{Optional named list; entity state after the transition.
\code{NULL} unless the TrajectoryLogger is configured to capture it.}

\item{condition_met}{Logical scalar or \code{NULL}; whether the \code{condition}
predicate was satisfied. \code{TRUE} when condition passed or was absent,
\code{FALSE} for audit records emitted when condition vetoed the policy call.}

\item{reward}{Optional numeric or named list; reward signal(s).}
}
\value{
A list of class \code{"TrajectoryRecord"}.
}
\description{
Engine-owned audit record emitted at each decision point when a
\code{TrajectoryLogger} is configured. This is the canonical surface for policy
evaluation, audit, and RL reward computation.
}
\details{
\code{state_before} and \code{state_after} are \code{NULL} by default. Supply a
\code{summary_fn} to the TrajectoryLogger to enable summary-level or full capture.
}
